<%= form_for [:admin, @concert] do |f| %>

  <% if @concert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
      <% @concert.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= app_text_field f, :name %>
  <%= app_text_field f, :date_and_time %>
  <%= app_text_field f, :ident_string %>
  <%= app_text_field f, :suggested_ticket_donation %>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
